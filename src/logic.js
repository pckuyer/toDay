const todoEntry = (inputText) => {
	//no dom manipulation here
	//no css here
	// just write what an entry is / does
	//also, think funfunfunctions: how will I go about inheritance?

	return { inputText, inputDate };
};
